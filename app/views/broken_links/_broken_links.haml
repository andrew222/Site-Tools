%table.broken_links_tab
  %tr
    %th URL
    %th dom_elem
  - @broken_links.each do |broken_link|
    %tr
      %td
        = broken_link.url
      %td
        = broken_link.dom_elem
= will_paginate @broken_links, renderer: BootstrapPagination::Rails, previous_label: t("links.pre_page"), next_label: t("links.next_page")
